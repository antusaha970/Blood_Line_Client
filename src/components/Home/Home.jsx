import { Container } from "@mui/material";
import { Header } from "../index";

const Home = () => {
  return (
    <Container maxWidth="lg">
      <Header />
    </Container>
  );
};

export default Home;
